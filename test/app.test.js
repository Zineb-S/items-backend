test('Sample test', () => {
    expect(1 + 1).toBe(2);
  });
  
  // Example for Mocha
  const assert = require('chai').assert;
  describe('Sample Test', () => {
    it('should return true', () => {
      assert.equal(1 + 1, 2);
    });
  });
  